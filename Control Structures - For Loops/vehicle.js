/* Create an array with 5 elements named as car types.
Use a loop  to index and value of each element.
Use console.log to display the array on the console.
*/

// Create an array named 'vehicles' and add 5 different car types
let vehicles = ["BMW", "Audi", "Ford", "Volkswagen", "Mercedes"];

// Use a loop to go through each car type in the 'vehicles' array
for (let i = 0; i < vehicles.length; i++) {
    // Print a message for each car type
    console.log("I would love to drive a " + vehicles[i]);
}
